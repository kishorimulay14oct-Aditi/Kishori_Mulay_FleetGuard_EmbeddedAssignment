; FleetGuard ESP32 Firmware
; PlatformIO project configuration

[env:esp32dev]

platform = espressif32
board = esp32dev
framework = arduino

monitor_speed = 115200

build_flags =
    -I firmware/include

src_dir = firmware/src
